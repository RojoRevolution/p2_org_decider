<% for(let i=0; i < ideaName.length; i++) { %>
    <div class="card m-2 card-adjust">
        <div class="card-body">
            <h5 class="card-title">
                <%= ideaName[i]%>
            </h5>
            <btn class="btn btn-secondary btn-sm">Put Up For Vote</btn>
        </div>
    </div>
    <% }; %>